import java.util.Scanner;
public class SampleCode {
    
    // create an odd even
    // what do u need? method for returning if odd or even
    // scanner
    // method for user
    
    public static void main(String [] args) {
        getUser();
        getUser();
        getUser();
    }
    
    public static void getUser(){
        Scanner user = new Scanner(System.in);
        
        System.out.println("Enter a value");
        int user1 = user.nextInt();
        
        // pls do not forget this part my pookiebear
        // the code will not work kapag di mo to kinall
        user1 = myConditions(user1);
            if (user1 == 1) {
                System.out.println("The number is odd");
            } else {
                System.out.println("The number is even");
            }
    }
    public static int myConditions(int x){
        if (x % 2 == 0) {
            return 0;
        } else {
            return 1;
        }
    }
}
